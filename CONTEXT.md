# Agent Inbox

Agent Inbox 是 agent 工作的关注与跟进上下文，用会话状态帮助用户及时处理已完成工作，而不是通用任务管理器。

## Language

**运行中会话**:
Agent 当前仍在执行工作的会话。
_Avoid_: 活跃待办、进行中任务

**待办**:
需要用户采取行动的会话，包括尚未回答的选择、审批或输入请求，以及工作结束后的确认。所有待办进入同一个列表和计数，明确展示待处理内容。
_Avoid_: 普通任务、历史记录

**新待办**:
已观察的会话首次需要处理，或在待回复与待确认之间切换，产生新的待办提醒。应用启动时直接发现的已有待办不属于新待办。
_Avoid_: 新任务、新会话

**等待你处理**:
待办的一种：Agent 已发出选择、审批、权限或输入请求，需要用户回到会话处理。未回答问题不会因为后续输出、工作结束或应用重启而消失；处理后自动更新，不能用确认完成代替回答。
_Avoid_: 把普通输出当作待办、仅用颜色表达需要处理、把待回复排除在待办之外

**会话源**:
会话数据的来源种类（Codex、Grok、Claude Code）。列表可混排多源，用标签区分。
_Avoid_: 模型名、账号、安装路径

**会话身份**:
由会话源 + 源原生会话 ID 共同标识一条会话。源原生 ID 保持各工具原格式；完成确认等持久化使用二者的复合键。
_Avoid_: 只靠文件名、只靠项目名

**Grok 待办**:
Grok 一轮工作已结束（`turn_ended`），agent 停住等待用户下一步提示，仍待用户在 Inbox 确认。进程是否仍存活不改变待办语义——交互式 TUI 常态就是进程活着等输入。
_Avoid_: 必须等进程退出才算待办、会话关闭才提醒

**Claude Code 会话**
Claude Code 生成的可继续对话。一轮工作停止并等待下一步输入属于待办；进程退出不是待办定义。
